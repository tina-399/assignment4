import React from 'react'

const LoginPage = () => {
    return (
        <div>
            <h1>Login</h1>
            <button onClick={('Login')}>Login</button>
        </div>
    )
}

export default LoginPage;
